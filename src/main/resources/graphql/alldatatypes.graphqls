type Sample {
    id: ID!
    name: String!
    age: Int
    height: Float
    isTall: Boolean


}

interface Animal{
    legs: Int
    hasFeather: Boolean
    color: Color!
}

type Dog implements Animal {
    legs: Int,
    hasFeather: Boolean
    color: Color!
    isBark: Boolean
}

type Duck implements Animal {
    legs: Int!
    hasFeather: Boolean!
    color: Color!
    isQuack: Boolean!
    wiggleButt: Boolean!
}

enum Color {
  BROWN, TAN, DARK_BROWN, AMBER
}