spring:
  application:
    name: graphql-course-demo

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  graphql:
    graphiql:
      enabled: true
    websocket:
      path: /subscriptions

  threads:
    virtual:
      enabled: true

populate:
  table:
    enabled: false