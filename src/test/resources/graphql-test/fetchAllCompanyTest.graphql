query fetchAllCompanyTest {
    fetchAllCompany {
        success
        message
        companies {
            uuid
            name,
            rcNumber
            headOffice
            country
            businessType
        }
    }
}